package midtermExam;

/**
 * 
 * @author Samuel Shapiro <samuel.shapiro123@gmail.com>
 *
 */
public class QuestionOne {

	/**
	 * Which of the following are class variables? which are instance variables?
	 * public class Computer {
	 * 		public static long serialNumber = 48gn49v84-243;
	 * 		private int type = 45;
	 * 		protected String name = "Not Indicated;
	 * 		private static float speed 3.0f;
	 * }
	 */
	
	/*
	 * serialNumber and speed are class variables. Since they are static, changes
	 * made to them in one class instance are made to them in every instance.
	 */
	
	/*
	 * type and name are instance variables. Since they are not static, each instance of
	 * the class can have different values for the variables.
	 */
	
}
